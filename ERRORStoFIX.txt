Cannot update a component (`MyEditor`) while rendering a different component (`ForwardRef`). To locate the bad setState() call inside `ForwardRef`, follow the stack trace as described in 

WebSocket connection to 'wss://announcements-portal-frontend-production-e3c2.up.railway.app:8080/ws' failed: 